class document:

    # constructor for the document class
    def __init__(self, name, path):
        self.name = name
        self.path = path

    # get the document name
    def get_name(self):
        return self.name

    # load variables that are specified in the document
    def load_variables(self):
        # load the variables from the document
        pass
